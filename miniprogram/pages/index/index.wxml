<view class="container" >
  <block wx:if='{{ status === "PENDING" }}'>
    <skeleton cover='{{ false }}' />
  </block>
  <block wx:if='{{ status === "SUCCESS" }}'>
    <view class="header" >
      
    </view>
  </block>
</view>
